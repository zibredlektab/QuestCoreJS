<?xml version="1.0" encoding="UTF-8"?>
<QuestCoreGame>
	<title>ValentinesQuest</title>
	<starting-area>area1</starting-area>
	<starting-room>kitchen</starting-room>
	<starting-direction>n</starting-direction>


	<switches>
		<switch id="digit0" min="0" max="9">0</switch>
		<switch id="digit1" min="0" max="9">0</switch>
		<switch id="digit2" min="0" max="9">0</switch>
		<switch id="digit3" min="0" max="9">0</switch>
		<switch id="digit4" min="0" max="9">0</switch>
		<switch id="digit5" min="0" max="9">0</switch>
	</switches>

</QuestCoreGame>

<!-- I'll need either a separate structure to store switch attributes, or the switch structure
	needs to become multi-dimensional (ids map to objects which map "value" and "attributes",
	which is an object mapping ids to values)

	only the switch values need to be stored when saving, though -->
